// Gestor de transacciones para el banco

/*
void saveNewCliente()
{
    FILE *userdb = fopen("userdb.txt", "a+");
    cout << userdb;
    if (userdb != NULL)
    {
        fwrite(client, sizeof(cliente), 3, userdb);
        fclose(userdb);
        cout << "Archivo creado exitosamente." << endl;
    }
    else
    {
        cout << "No se pudo crear el archivo." << endl;
    }
}
*/